# Visit https://dash.cloudflare.com/profile/api-tokens
# Permissions required: Zone → DNS → Edit
#                       Zone → Zone → Read
CF_DNS_API_TOKEN=<TOKEN>
# Home Assistant long-lived token. Go to user profile -> security to configure
HA_TOKEN=<TOKEN>